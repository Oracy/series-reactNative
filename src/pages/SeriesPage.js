import React from 'react'
import { StyleSheet, View, Text } from 'react-native'

const SeriesPage = ({ series }) => (
  <View>
    <Text>
      Esta eh a SeriesPage
    </Text>
  </View>
)

const styles = StyleSheet.create({

})

export default SeriesPage